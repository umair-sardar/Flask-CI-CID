pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Code already checked out'
            }
        }

        stage('Build') {
            steps {
                sh 'pwd'
                sh 'ls'
            }
        }
    }
}
